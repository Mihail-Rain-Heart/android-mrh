package com.android.mrh.utils.baseAdapter.view_holders

import androidx.recyclerview.widget.RecyclerView.ViewHolder
import androidx.viewbinding.ViewBinding
import com.android.mrh.utils.baseAdapter.BaseItem

class SettingsViewHolder(private val binding: ViewBinding) : ViewHolder(binding.root) {
    fun bind(item: BaseItem) {

    }
}